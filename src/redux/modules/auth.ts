const initialState = {
  customerId: "6069e0c5325f750979ae2e25",
};

export default function reducer(state = initialState, action: { type: any }) {
  switch (action.type) {
    default:
      return state;
  }
}
